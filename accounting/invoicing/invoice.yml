Suggestion:
  id: serial32
  cashflow: bank\Cashflow
  operation: journal\Operation
  status: enum(WAITING, REJECTED, VALIDATED, OUT) = WAITING
  reason: set(AMOUNT, THIRD_PARTY, REFERENCE, DATE)
  weight: int16(0, NULL)
  createdAt: datetime = SPECIAL(now)
  UNIQUE: [
    ["cashflow", "operation"]
  ]
